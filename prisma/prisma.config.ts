// Prisma 7.x configuration
// This file is kept for reference but the actual configuration
// is now handled directly in the PrismaClient constructor

export const databaseUrl = process.env.DATABASE_URL;
