'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function OptionsAnalysis({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Options Analysis (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
