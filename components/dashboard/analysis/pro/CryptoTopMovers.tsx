'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function CryptoTopMovers({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Crypto Top Movers (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
