'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function FuturesAnalysis({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Futures Analysis (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
