'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function ForexAnalysis({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Forex Analysis (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
