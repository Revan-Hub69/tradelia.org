'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function CryptoDepthAggregated({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Crypto Depth Aggregated (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
