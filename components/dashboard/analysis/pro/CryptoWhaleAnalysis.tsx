'use client';

import React from 'react';

interface Props {
  isPro: boolean;
}

export default function CryptoWhaleAnalysis({ isPro }: Props) {
  return (
    <div className="text-sm text-text-tertiary">Crypto Whale Analysis (placeholder) — componente Pro non disponibile in questa build.</div>
  );
}
