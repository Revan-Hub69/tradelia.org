#!/usr/bin/env sh

HUSKY_INTERNAL="$(dirname "$0")/_/husky.sh"
if [ -f "$HUSKY_INTERNAL" ]; then
  . "$HUSKY_INTERNAL"
fi

SCRIPT_PATH="scripts/auto-version.js"

if [ -f "$SCRIPT_PATH" ]; then
  echo "[husky] eseguo auto-version"
  node "$SCRIPT_PATH"
else
  echo "[husky] auto-version non trovato, push consentito"
fi
