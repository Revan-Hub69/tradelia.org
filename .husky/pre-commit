# Run lint-staged for staged files
npx lint-staged

# Generate tokens if tokens.json changed
if git diff --cached --name-only | grep -q "design-tokens/tokens.json"; then
  npm run generate-tokens
  git add design-tokens/tokens.css
fi
